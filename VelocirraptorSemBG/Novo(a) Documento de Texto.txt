void setup()
{
  size(1280, 720, P3D);
  for (int i = 0; i < 73; i++) {
    // Formate o número do arquivo com zeros à esquerda (por exemplo, 001, 002, 010, etc.)
    //String filename = nf(i + 1, 3) + ".jpg";
    images[i] = loadImage("001.png");
    //String filename = nf(i + 1, 3) + ".jpg";
    //PImage img = loadImage(filename);
    //images[i] = removeWhiteBackground(img);
  }

}